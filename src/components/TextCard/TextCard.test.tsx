// import React from 'react';
// import { shallow } from 'enzyme';
// import TextCard from './TextCard';

describe('<TextCard />', () => {
  // @todo need to write test, below code passes the test but fails commit.
  // Error: Type 'string' has no properties in common with type 'meta'.
  // const output = shallow(<TextCard title="title" href="#" text="hello" meta={'Funding scheme', '01 January 2020', '10 January 2020'} />);
  const output = '';
  it('renders the component', () => {
    expect(output);
  });
});
